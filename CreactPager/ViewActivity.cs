
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Android;
using Android.App;
using Android.Content;
using Android.OS;
using Android.Runtime;
using Android.Views;
using Android.Widget;

namespace CreactPager
{
	//[Activity(Label = "ViewActivity")]
	//public class ViewActivity:Activity
	//{
	//	protected override void OnCreate(Bundle savedInstanceState)
	//	{
	//		base.OnCreate(savedInstanceState);
	//		SetContentView(Resource.Layout.HomeLayout);
	//		var listView = FindViewById<ListView>(Resource.Id.List);
	//		var butDelete = FindViewById<ImageButton>(Resource.Id.imagebut);
	//		var butStar = FindViewById<ImageButton>(Resource.Id.imageButton1);
	//		var docsFolder = System.Environment.GetFolderPath(System.Environment.SpecialFolder.MyDocuments);
	//		var pathToDatabase = System.IO.Path.Combine(docsFolder, "db_drinks.db");
	//		Person[] persons = MyDataBase.GetNames(pathToDatabase);
	//		var adapter = new MyAdapter(this, persons, pathToDatabase,listView);

	//		listView.Adapter = adapter;

	//	}

	//}
	
}

